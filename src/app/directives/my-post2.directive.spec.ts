import { MyPost2Directive } from './my-post2.directive';

describe('MyPost2Directive', () => {
  it('should create an instance', () => {
    const directive = new MyPost2Directive();
    expect(directive).toBeTruthy();
  });
});
